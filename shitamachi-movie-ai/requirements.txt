# shitamachi-movie-ai — YouTube動画生成パイプライン
# Python 3.11+
#
# 外部APIはClaude（台本生成）のみ。画像はCodex（ChatGPT定額枠）、
# 音声・字幕・編集・書き出しはCanva（定額枠）に委譲しているため、
# 動画編集系ライブラリ（moviepy等）・TTS系SDKは使わない。

# ─── LLM ───
anthropic>=0.40.0          # ステップ1: リサーチ+台本生成（Web検索ツール対応）

# ─── 環境変数 ───
python-dotenv>=1.0.1       # .env からAPIキーを読み込む

# ─── 画像検収 ───
Pillow>=10.4.0             # ステップ2: 納品画像の解像度・アスペクト比チェック

# ─── ユーティリティ ───
pydantic>=2.9.0            # 台本JSONのスキーマ検証（LLM出力の型崩れ対策）
tenacity>=9.0.0            # APIリトライ（レート制限・一時エラー対策）
